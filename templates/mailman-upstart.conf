description "mailman"
start on runlevel [2345]
stop on runlevel [06]

script
    sudo -u mailman -H bash -c "{{ mailman_bundler_path }}/bin/mailman start"
end script

pre-stop script
   sudo -u mailman -H bash -c "{{ mailman_bundler_path }}/bin/mailman stop"
end script
